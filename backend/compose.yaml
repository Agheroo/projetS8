services:
  backend:
    build: .
    ports:
      - 8080:8080
    depends_on:
      - db
  db:
    image: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=ThePassword
    ports:
      - 3306:3306
    volumes:
      - data:/var/lib/mysql
volumes:
  data:
